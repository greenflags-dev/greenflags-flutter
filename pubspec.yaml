name: greenflags
description: >-
  Dart/Flutter SDK for GreenFlags feature flags. Snapshot + cache reads,
  polling, geofence evaluation and offline persistence — evaluated at the edge.
version: 0.1.0
homepage: https://greenflags.dev
repository: https://github.com/greenflags-dev/greenflags-flutter
documentation: https://greenflags.dev/docs/

environment:
  sdk: ^3.4.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
